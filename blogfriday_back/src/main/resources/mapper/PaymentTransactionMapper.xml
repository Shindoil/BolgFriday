<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.blogfriday.payment.repository.PaymentTransactionRepository">
    <select id="findAll" resultType="com.blogfriday.payment.dto.PaymentTransactionDTO">
        SELECT * FROM PaymentTransactions
    </select>

    <select id="findById" parameterType="int" resultType="com.blogfriday.payment.dto.PaymentTransactionDTO">
        SELECT * FROM PaymentTransactions WHERE transaction_id = #{transactionId}
    </select>

    <insert id="insert" parameterType="com.blogfriday.payment.dto.PaymentTransactionDTO">
        INSERT INTO PaymentTransactions (method_id, transaction_date, is_successful, order_id, amount)
        VALUES (#{methodId}, #{transactionDate}, #{successful}, #{orderId}, #{amount})
    </insert>

    <update id="update" parameterType="com.blogfriday.payment.dto.PaymentTransactionDTO">
        UPDATE PaymentTransactions
        SET method_id = #{methodId}, transaction_date = #{transactionDate}, is_successful = #{successful},
            order_id = #{orderId}, amount = #{amount}
        WHERE transaction_id = #{transactionId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM PaymentTransactions WHERE transaction_id = #{transactionId}
    </delete>
</mapper>